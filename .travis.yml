language: node_js

node_js:
    - 4
    - 5
    - 6

notifications:
    email: false

cache:
    apt: true
    directories:
        - $HOME/.npm
        - $HOME/.cache/bower

addons:
    firefox: latest
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-4.9

env:
    - CXX=g++-4.9

before_install:
    - npm install -g bower

before_script:
    - cd example && bower install --verbose && cd ..

script:
    - npm run server &
    - npm test
